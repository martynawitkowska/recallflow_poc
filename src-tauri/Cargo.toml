[package]
name = "recallflow"
version = "0.1.0"
description = "A local-first study application"
edition = "2021"

[lib]
name = "recallflow_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
